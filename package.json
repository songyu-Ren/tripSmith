{
  "name": "tripsmith",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "lint": "npm -w apps/web run lint && npm -w packages/shared run lint",
    "typecheck": "npm -w apps/web run typecheck && npm -w packages/shared run typecheck",
    "format": "npm -w apps/web run format && npm -w packages/shared run format"
  }
}

